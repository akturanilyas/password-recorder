class ValidationRegEx {
  const ValidationRegEx();
  // TODO change RegEx
  static const String username = r'^[a-zA-Z0-9]+(\s[a-zA-Z]+)?$';
  static const String email = r'^[a-zA-Z0-9]+(\s[a-zA-Z]+)?$';
  static const String password = r'^[a-zA-Z0-9]+(\s[a-zA-Z]+)?$';
}
